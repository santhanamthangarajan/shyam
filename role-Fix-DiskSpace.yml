---
- hosts: all #"{{survey_host}}"
  gather_facts: no
  become_user: root
  become: yes
  strategy: free
          
  tasks:
  - name: include Fix-DiskSpace
    include_role: 
     name: Fix-DiskSpace
  
